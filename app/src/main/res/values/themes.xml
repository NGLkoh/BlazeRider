<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Application light theme using Material 3 -->
    <style name="Theme.AORVBlazeRider" parent="Theme.Material3.Light.NoActionBar">
        <!-- Optional: Customize colors -->
        <item name="colorPrimary">@color/red_orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorAccent">@color/quit</item>

        <!-- Disable dark mode fallback -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="Theme.SearchTransparent" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

</resources>